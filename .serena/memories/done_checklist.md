When finishing a coding task, prefer running the relevant subset of verification and ideally the full project check when practical: `npm run pretty`, `npm run lint`, `npm run test:run`, and `npm run build`. For local dev, ensure `.env` exists and `database/` exists before starting the server. Be careful with event-scoped score flows, queue side effects, and bracket advancement when changing scoring logic.