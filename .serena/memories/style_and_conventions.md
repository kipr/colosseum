Codebase conventions: TypeScript across client/server, React function components with hooks, Express routers/services on the backend, JSON-backed template schemas for scoresheets. ESLint uses `@eslint/js`, `typescript-eslint`, `eslint-plugin-react`, and CSS linting. Existing code commonly uses single quotes, semicolons, async/await, and explicit interface/type aliases for route payloads and UI models. Domain model is strongly event-centric. Scores are currently classified as `seeding` or `bracket`; bracket flows assume two explicit team slots (`team1_id`, `team2_id`). Preserve existing event-scoped workflow patterns and avoid broad schema changes unless necessary.