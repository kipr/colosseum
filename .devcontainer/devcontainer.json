{
  "name": "colosseum-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "mounts": [
    "source=/var/home/tom/.config/codex,target=/home/node/.codex,type=bind,consistency=cached",
    "source=/var/home/tom/.serena,target=/home/node/.serena,type=bind,consistency=cached",
  ],
  "workspaceFolder": "/workspaces/colosseum",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/colosseum,type=bind,consistency=cached",
  "forwardPorts": [3000, 5173],
  "runArgs": ["--security-opt", "label=disable"],
  "remoteUser": "node"
}
