---
description: 
alwaysApply: true
---

Make sure to verify all changes by running `npx eslint src` and `npm run build` until you have eliminated all errors caused by your changes (ignore others). When all errors have been addressed, run `npx prettier src --write` to format your changes.
